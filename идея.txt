Так, ну вроде план понятен. Давай еще раз определимся с последовательностью что и как мы делаем. При этом нужно грамотно вписать новый функционал в программу. Желательно сделать гибкую структуру классов согласно принципам ООП, грамотно вписать все в интерфейс программы, чтобы пользователю было удобно работать. Все должно выглядеть аккуратно, работать правильно, а код строго и четко оформлен. Пока опять ничего не меняем, только рассуждаем.
17:08

Да, давай зафиксируем архитектурно. Я бы не делал это “приклеенным Python-портом”, а встроил как отдельный аккуратный подсистемный слой: компиляция FPGA-модели → генерация пакетов → отправка/симуляция/дамп.
Главный Пайплайн
```mermaid
flowchart LR
    A["Редакторские объекты"] --> B["Compiled FPGA model"]
    B --> C["FpgaPacketCompiler"]
    C --> D["Список пакетов"]
    D --> E["Serial Transport"]
    D --> F["Packet Dump"]
    D --> G["FPGA Simulator"]
```
Смысл: редактор продолжает жить своей жизнью: ImageObject, TextObject, RotationObject, StaticGroupObject. А перед отправкой мы получаем строгую FPGA-модель, как будто уже экспортировали XML, и только из нее делаем пакеты.
1. Сначала Делаем Модель, А Не UI Первый слой - получить из текущего проекта “аппаратное представление”.
Я бы ввел классы примерно такого уровня:
FpgaCompiledDocument
FpgaCompiledObject
FpgaCompiledParameter
FpgaCompiledMemoryBlock
FpgaCompiledDocument должен содержать:
ширина/высота/background
список объектов в аппаратном порядке
для каждого объекта:
  type
  iparams
  data
  startParamIndex
  paramCount
  memId, если есть память
Это фактически C++-аналог того словаря, который сейчас строит vbXML.py, только аккуратный, типизированный и встроенный в программу.
Важно: этот слой должен переиспользовать текущую логику экспорта из ProjectManager, потому что там уже есть превращение ImageObject в staticgroup/rotationobject, TextObject в font/text и так далее. Не надо дублировать эту логику в packet compiler.
2. Потом Генератор Пакетов Второй слой - чистый генератор пакетов. Он не должен знать про UI, QPainter, панели, выделение объектов и прочее.
Примерная структура:
FpgaPacket
FpgaPacketCompiler
FpgaPacketFramer
FpgaChecksum
FpgaParameterEncoder
FpgaStaticMemoryEncoder
FpgaRotationMemoryEncoder
Разделение такое:
	•	FpgaPacketFramer отвечает за SAIN, длину, тип пакета, FCS.
	•	FpgaChecksum считает Fletcher32.
	•	FpgaParameterEncoder делает пакет 0x03.
	•	FpgaStaticMemoryEncoder делает данные и пакеты 0x04.
	•	FpgaRotationMemoryEncoder делает 9×9 тайлы и пакеты 0x02.
	•	FpgaPacketCompiler собирает все вместе в правильной последовательности.
Главное правило: C++-пакеты должны побайтово совпадать с Python-пакетами из [CompilePackets.py](/Users/sergejasin/Documents/AvionixDesigner/FrameGUI/CompilePackets.py).
3. Сначала Побайтовая Совместимость До красивого UI и симулятора я бы сделал “техническую правду”:
один XML
Python генерирует пакеты
C++ генерирует пакеты
сравниваем byte-by-byte
Проверить нужно особенно:
magic SAIN
length
type
obj_id
mem_id
address
payload
Fletcher32
rotationobject 31-byte tiles
staticgroup chunks по 1024
Если этот слой надежный, все остальное уже безопасно строить сверху.
4. Транспортный Слой Пакетам все равно, куда идти. Поэтому нужен интерфейс:
class FpgaTransport {
public:
    virtual bool sendPacket(const FpgaPacket& packet) = 0;
};
Реализации:
SerialFpgaTransport       отправляет на реальную ПЛИС
PacketDumpTransport       сохраняет пакеты в файл
SimulatorFpgaTransport    передает пакеты внутреннему симулятору
Это даст гибкость: одна и та же кнопка “скомпилировать” дает один и тот же список пакетов, а дальше пользователь выбирает цель.
5. Интерфейс Пользователя Я бы добавил отдельную панель/док “ПЛИС” или “Пакеты”.
В ней:
[Скомпилировать]
[Отправить на ПЛИС]
[Отправить в симулятор]
[Сохранить дамп]
Ниже таблица пакетов:
№
Тип: parameter / rotation memory / static memory / layer3d
Object
obj_id
mem_id
address
payload size
FCS
status
Это будет удобно: пользователь видит, что реально уйдет на плату. Не магическая кнопка “отправить”, а прозрачный список.
Дополнительно полезны статусы:
пакетов всего
байт всего
ошибки компиляции
предупреждения
последняя отправка
6. Симулятор Симулятор лучше делать не сразу как “рисовалку объектов”, а как получателя пакетов.
То есть:
пакеты 0x03 кладут iparam в состояние симулятора
пакеты 0x02 кладут rotation BRAM
пакеты 0x04 кладут static BRAM
потом SimulatorRenderer рисует кадр
Классы:
FpgaSimulator
FpgaSimulatorState
FpgaPacketDecoder
FpgaSoftwareRenderer
Так мы проверяем именно то, что ушло бы на ПЛИС. Это мощнее, чем просто повторить draw() объектов редактора.
В UI я бы сделал переключатель предпросмотра:
Редактор / Симулятор
Редакторский viewport остается быстрым и удобным для работы. Симуляторский viewport показывает “как это должно выглядеть после загрузки пакетов”.
7. Последовательность Работ Я бы шел так:
	1	Описать внутреннюю FpgaCompiledDocument модель.
	2	Вынести из ProjectManager получение compiled FPGA document без обязательной записи XML на диск.
	3	Реализовать FpgaPacketFramer: SAIN, length, packet type, Fletcher32.
	4	Реализовать 0x03 parameterator.
	5	Реализовать 0x04 staticgroup memory.
	6	Реализовать 0x02 rotationobject memory, отдельно и очень тщательно.
	7	Сделать golden-тесты против Python.
	8	Добавить UI-панель пакетов без отправки на железо: только “скомпилировать” и посмотреть список.
	9	Добавить dump в .bin/hex.
	10	Добавить serial-отправку.
	11	Добавить packet-level simulator.
	12	Постепенно расширять simulator renderer по модулям: rectangle, rectangle_a, staticgroup, rotationobject, aviahorizont, dashed_line, ribbon scale, text/font.
Ключевая Архитектурная Мысль Новый функционал должен быть не “частью MainWindow”, а отдельной подсистемой:
src/fpga/model
src/fpga/packets
src/fpga/transport
src/fpga/simulator
src/ui/panels
Тогда код останется строгим: UI только показывает и вызывает действия, packet compiler только компилирует, transport только отправляет, simulator только принимает пакеты и рисует. Это как раз тот случай, где аккуратная ООП-структура реально спасет проект от каши.

17:09
Так, ну мне все нравится, давай приступать к работе по твоему плану. Идея с отдельной панелью под ПЛИС мне нравится, но я бы сделал немного не так. Будет одна панель, которая отвечает за выбор стриминга (uart, bin для отладки и кнопка запуска симулятора). Также симулятор я бы тоже сделал отдельной панелью, чтобы как по твоему плану можно было запустить режим src/fpga/simulator и в реальном времени получать изменения (двигаем что-то в рабочей области, сразу видим). Я бы в этой панели сделал на фон такую как рамку экрана, чтобы было как будто мы смотрим результат на экране ПЛИС, было бы красиво. По структуре и схеме классов я доверяюсь тебе, главное сделай все грамотно, чисто и правильно.
